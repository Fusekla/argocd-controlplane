apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
- namespace.yaml
- argocd-install/install-v3.1.0.yaml

patches:
  - ../config/argocd-cm.patch.yaml
  - ../config/argocd-rbac-cm.patch.yaml

labels:
- includeSelectors: false
  pairs:
    app.kubernetes.io/managed-by: gitops
